# 验证记录

[返回主页](../README.md) · [复现命令](getting-started.md)

验证日期：2026-09-21。范围：目录迁移、构建入口、测试路径与项目文档；控制算法、外设参数和通信协议未作修改。

## 软件验证

| 检查 | 环境 / 范围 | 结果 |
| --- | --- | --- |
| Python 回归 | Python 3.14.4；`unittest discover -s tests -p "*_test.py"` | 39 项通过 |
| 宿主 C 协议 | MSVC 14.50；CMake 独立宿主工程；Debug | 1 个 CTest 通过，包含 3 个协议检查函数 |
| STM32 Debug | ARM GCC 14.3.1、CMake 4.2.3、Ninja 1.13.2；全新构建目录 | 成功生成 `CAR_FIRST.elf` |
| STM32 Release | 同上；全新构建目录 | 成功生成 `CAR_FIRST.elf` |

C 协议检查分别覆盖命令帧往返、损坏 CRC 拒绝和遥测布局。Python 测试保留迁移前的断言，仅调整文件定位，包含 MaixCAM 行为测试和 STM32 源码静态检查。

链接器报告的静态资源分配如下，不代表运行时任务栈峰值或最坏执行时间：

| 配置 | FLASH 分配 | RAM 分配 |
| --- | ---: | ---: |
| Debug | 55,984 B / 256 KB | 28,064 B / 48 KB |
| Release | 34,708 B / 256 KB | 28,048 B / 48 KB |

测试与固件命令见[构建指南](getting-started.md)。测试产物和固件输出均在忽略的 `build/` 目录中，不作为源码提交。

## 迁移完整性

- 对比迁移前远端基线：176 个活动固件文件及 MaixCAM 主程序内容保持一致（忽略检出时换行差异）。
- 84 处本地 Markdown 链接目标均存在；Python 源码语法检查通过。
- Git 暂存差异检查通过；缓存、ZIP 打包文件与构建产物不在跟踪清单中。
- 恢复的启动文件仅清理一处行尾空白，链接脚本保持原始内容。

## 实车结果与验证边界

作者确认项目由其独立开发并完成整车集成，已经跑通完整赛道。目前仓库未收录公开实车录像、计时记录或竞赛成绩，因此不提供成功率、圈速或提升百分比。

本次整理没有重新烧录固件、在 MaixCAM 上运行应用、进行实车复测，亦未执行 CubeMX 图形界面再生成。宿主测试通过和固件编译成功，不等价于相机识别效果、雷达实测表现或实时调度性能已经重新验证。

MaixPy 设备版本和实际硬件清单尚未锁定。后续若补充录像、实验日志或成绩，应注明测试条件和版本，再同步更新中英文主页与简历描述。
